"""
API routers.

Each pipeline stage will eventually get its own router here
(incidents, evidence, investigations, actions, outcomes, audit).
Currently only health.py exists for connectivity checks.
"""
